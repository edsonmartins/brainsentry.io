# Test configuration

# Allow bean definition overriding for tests
spring.main.allow-bean-definition-overriding=true

# Logging
logging.level.root=INFO
logging.level.com.integraltech.brainsentry=DEBUG

# PostgreSQL database configuration for tests
spring.datasource.url=jdbc:postgresql://192.168.1.110:5444/brainsentry-test?ssl=false&serverTimezone=America/Sao_Paulo
spring.datasource.username=admin
spring.datasource.password=Rapidex@727204
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
